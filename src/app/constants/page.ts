import { IPage } from '@Interfaces/interfaces/route';

export const pages: IPage[] = [
  {
    path: 'dashboard',
    name: 'Dashboard',
  },
];
export const pageName = {
  ROOT: '/',
  LOGIN: '/login',
  DASHBOARD: '/dashboard',
};
